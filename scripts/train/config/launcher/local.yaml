# @package _global_
# Local launcher configuration (no SLURM)

defaults:
  - override /hydra/launcher: basic

n_gpus: 1
cpus_per_task: 8
cache_dir: /home/ddrous/Projects/Stable-WorldModel/.stablewm # Use stable-worldmodel default cache
wandb:
  enabled: True
  config:
    entity: ddroux
    project: world-models
    name: ${output_model_name}
    id: ${subdir}
    resume: allow
    log_model: False
